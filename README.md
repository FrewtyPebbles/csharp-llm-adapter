# AI Agent Adapter

This C# package is meant to act as a wrapper around multiple AI APIs such as OllamaSharp, Google.GenAI, and much more. This wrapper exposes a single shared interface for using any of these different APIs. I made this library for my own use hence the `local_publish.py` script.

## Backends Currently Implemented

 - **OllamaSharp**
